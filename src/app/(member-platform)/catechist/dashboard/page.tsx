// import { auth } from "@/lib/auth";
import React from "react";

const Page = async () => {
  // const session = await auth();
  //   console.log("session", session);
  return <div>Page</div>;
};

export default Page;
